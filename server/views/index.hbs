<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="{{ meta.charset }}"/>
    <title>{{ title }}</title>
    <meta name="description" content="{{ meta.description }}"/>
    <link rel="stylesheet" href="{{ staticBasePath }}index.css" media="screen"/>
</head>
<body>
<div class="home-page">
    {{> logo}}
    <div class="home_page__adventures">
        {{#if adventures}}
            {{#each adventures}}
                {{> adventure}}
            {{/each}}
        {{/if}}
    </div>
</div>
<script src="{{ staticBasePath }}js/client.js"></script>
</body>
</html>
